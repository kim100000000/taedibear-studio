[phases.setup]
nixPkgs = ["jdk17"]

[phases.build]
cmds = ["cd backend-java && chmod +x gradlew && ./gradlew build -x test"]

[start]
cmd = "cd backend-java && java -jar build/libs/taedibear-studio-backend-0.0.1-SNAPSHOT.jar"
