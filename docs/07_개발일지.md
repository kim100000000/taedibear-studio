# Taedibear Studio — 개발일지

**작성자:** @taedibear  
**최초 작성:** 2026-07-01

---

## 2026-06-27

### 프로젝트 초기 세팅 완료
- 구현 내용
  - 프로젝트 폴더 구조 생성 (`frontend/`, `backend/`, `docs/`)
  - `.gitignore`, `README.md` 작성
  - 요구사항 명세서 (`00_요구사항명세서.md`), 서비스 기획서 초안 작성
- 특이사항
  - MVP 범위 확정: 회원가입/로그인, 이미지 업로드, AI 캡션, 예약 업로드, 히스토리

### backend-node 백엔드 구현 (Node.js 시절)
- 구현 내용
  - Express 기반 MVC 구조 세팅
  - MySQL 연결, 공통 에러/응답 핸들러
  - User/Post/InstagramAccount/ScheduledPost 모델 정의
  - 회원가입/로그인 API (JWT, bcrypt)
  - 구글/네이버 소셜 로그인 (Passport.js)
  - 사용자 정보 API (`GET /api/users/me`)
  - 이미지 업로드 API (AWS S3)
  - AI 캡션 생성 API (Gemini `gemini-2.5-flash`)
  - 게시물 CRUD API
  - Meta API 연동 및 즉시 업로드
  - 예약 업로드 cron job (node-cron)
- 특이사항
  - Gemini 모델: 최초 `gemini-1.5` → `gemini-2.5-flash` 수정 (deprecated 대응)

---

## 2026-07-01

### Spring Boot 백엔드 전환
- 구현 내용
  - Node.js 백엔드 전체를 Spring Boot 3.x (Java 17)으로 재구현
  - Spring Security + JWT 인증
  - JPA 엔티티 (`User`, `Post`, `InstagramAccount`, `ScheduledPost`)
  - Google / Kakao / Naver OAuth (Spring WebClient 직접 호출, `NaverOAuthClient.java`)
  - 예약 발행 스케줄러: `@Scheduled(cron = "0 * * * * *")` — 1분 폴링
  - 개발 포트 4000 → 8080 변경
- 특이사항
  - Naver OAuth는 Spring Security OAuth2 미지원으로 `NaverOAuthClient.java` 직접 구현
  - `GET /api/instagram/connect` 인증: 브라우저 리다이렉트 특성상 `?token=<JWT>` 쿼리 파라미터 방식

### React TypeScript 전환 + 부가 기능
- 구현 내용
  - 기존 JavaScript React → TypeScript(strict) 전환
  - `axios` 401 인터셉터 추가 (세션 만료 자동 로그아웃)
  - `ErrorBoundary` 컴포넌트 추가
  - `ProtectedRoute` 로딩 상태 통일
  - 다크모드: CSS 커스텀 변수 (`:root` / `[data-theme='dark']`), `ThemeContext`, `ThemeToggle`
  - 플랜 UI: `PlanSection` 컴포넌트 (무료/프로 비교 카드)
  - 온보딩: `OnboardingModal` (4단계 슬라이드, 첫 로그인 1회 노출)
- 특이사항
  - 다크모드 초기값: `localStorage('theme')` → `prefers-color-scheme` 순으로 결정
  - 플랜 상태는 현재 `localStorage` 임시 저장 (결제 연동 전)

### i18n 다국어 지원 (한국어 / 영어)
- 구현 내용
  - `i18next` + `react-i18next` 설치 및 초기 설정
  - 번역 파일: `src/locales/ko.json`, `src/locales/en.json`
  - 모든 UI 문자열 `t()` 로 교체
  - `LangToggle` 버튼 (NavBar + 랜딩 헤더)
  - 언어 설정 `localStorage('lang')` 유지
- 특이사항
  - 배열 번역은 `{ returnObjects: true }` 옵션 사용 (온보딩 steps 등)
  - API로 전달하는 값(업종, 분위기)은 한국어 그대로 유지 (Gemini 프롬프트 호환)

### 기존 문서 동기화
- 구현 내용
  - `00_요구사항명세서.md` v1.2: Spring Boot 시스템 다이어그램, FR-111 네이버 로그인, 3.6 UI/UX 요구사항 추가
  - `02_기능명세서.md` v1.1: F-02 네이버, F-07 Spring @Scheduled, F-11~F-14 추가
  - `03_화면설계서.md` v1.1: 네이버 버튼, NavBar 토글, PlanSection, 컴포넌트 목록 갱신
  - `04_DB설계서.md` v1.1: cron → Spring @Scheduled 레퍼런스 수정
  - `05_API명세서.md` v1.2: 포트 8080, NaverOAuthClient 구현 방식, instagram/connect 인증 방식 명시
- 특이사항
  - 문서와 실제 코드 간 괴리가 커서 전체 재동기화 수행

### Phase 1-1: 콘텐츠 캘린더 (`/calendar`)
- 구현 내용
  - `CalendarPage.tsx` 신규 생성 (P-11)
  - 월간 캘린더 뷰 (7열 CSS Grid, 이전/다음 월 이동)
  - 날짜별 예약 게시물 썸네일 + 시간 표시
  - 상태별 색상 구분: 예약 중(파랑) / 완료(초록) / 실패(빨강)
  - 드래그&드롭 예약 시간 변경 (`PUT /api/scheduled/:id` 재활용)
    - `pending` 상태만 드래그 가능
    - 드롭 시 원본 HH:MM 유지, 날짜만 변경
    - 10분 이전 드롭 차단
  - 클릭 시 상세 모달 (이미지, 캡션, 상태 배지, 시간, 예약 취소)
  - NavBar에 "캘린더" 메뉴 추가
  - CSS: `--cal-*` 테마 변수 (라이트/다크)
  - 다국어: `calendar.*` 번역 키
  - 문서: `02` v1.2 (F-15), `03` v1.2 (P-11, 흐름도 업데이트)
- 특이사항
  - 새 백엔드 API 없음 — 기존 `GET /api/scheduled`로 충분 (post 정보 포함 반환)
  - `statusBadgeClass()`: ScheduleStatus → PostStatus 클래스 매핑으로 기존 CSS 재사용

### Phase 1-2: AI 이미지 편집 (`/upload/edit`)
- 구현 내용
  - `ImageEditorPage.tsx` 신규 생성 (P-12)
  - Canvas API 기반 실시간 편집 미리보기
  - 비율 크롭 4종: 원본 / 1:1 / 4:5 / 1.91:1 (중앙 기준 자동 계산)
  - 필터 프리셋 5종: 감성 / 밝음 / 빈티지 / 선명 / 따뜻함 (brightness/contrast/saturation 조합)
  - 슬라이더 3종: 밝기 / 대비 / 채도 (범위 0~200, 기본 100)
  - 편집 초기화 버튼
  - 내보내기: `canvas.toBlob()` → JPEG 92% → `POST /api/posts/upload` → CaptionPage
  - `UploadPage.tsx` 수정: S3 직접 업로드 제거, `/upload/edit`으로 이동
  - CSS: 에디터 칩 버튼, 슬라이더, 반응형 레이아웃 추가
  - 다국어: `editor.*` 번역 키
  - 문서: `00` v1.3 (FR-605~608), `02` v1.3 (F-16), `03` v1.3 (P-12, 흐름도)
- 특이사항
  - 새 백엔드 API 없음 — 브라우저 Canvas에서 모든 편집 처리 후 기존 업로드 API 사용
  - 업로드 플로우 변경: UploadPage → **ImageEditorPage** → CaptionPage
  - `canvas.filter = "brightness(B%) contrast(C%) saturate(S%)"` 방식 (CSS filter 활용)
  - 커밋 시 `.git/HEAD.lock` / `index.lock` 이슈 발생 → 수동 커밋 필요 (macOS git maintenance 프로세스 충돌)

### Phase 2-1: 결제 연동 + 플랜 제한
- 구현 내용
  - **백엔드:** `Payment` 엔티티, `PaymentStatus` enum, `PaymentRepository` 신규 생성
  - `PaymentService` — 토스페이먼츠 결제 승인/취소 (WebClient), 플랜 제한 체크, 사용량 조회
  - `PaymentController` — `POST /confirm`, `GET /status`, `POST /cancel`
  - `UserController` — `GET /api/users/me/usage` 추가 (plan, used, limit, reset_at)
  - `PostService.createPost()` — Free 플랜 10회 초과 시 403 ApiException 발생
  - `application.yml` — `app.toss.secret-key`, `app.toss.client-key` 설정 추가
  - **프론트엔드:** `api/payments.ts` 신규 (confirmPayment, getPaymentStatus, cancelPayment)
  - `api/users.ts` — `getUsage()` 추가
  - `components/UpgradeModal.tsx` 신규 (한도 초과 시 Pro 유도 모달)
  - `components/PlanSection.tsx` 전면 개편 — localStorage 제거, 서버 plan 사용, 토스 결제 위젯 연동
  - `pages/PaymentSuccessPage.tsx`, `pages/PaymentFailPage.tsx` 신규 (토스 리다이렉트 처리)
  - `pages/DashboardPage.tsx` — `getUsage()` 호출, "X / 10회" 사용량 카드, 한도 도달 시 경고 색상
  - `pages/CaptionPage.tsx` — 403 응답 시 `UpgradeModal` 노출
  - `pages/SettingsPage.tsx` — 결제 완료 후 planRefresh 트리거
  - `App.tsx` — `/payments/success`, `/payments/fail` 라우트 추가
  - `index.html` — 토스 SDK `<script>` 추가
  - i18n: `plan.*` 키 다수 추가 (ko/en) — 가격 ₩9,900 → ₩29,000 수정
  - CSS: 사용량 경고 카드, 업그레이드 모달 스타일
  - 문서: `02` v1.5 (F-18), `04` v1.2 (payments 테이블), `05` v1.4 (결제 API)
- 특이사항
  - 토스 secret-key 미설정 시 API 호출 스킵 (개발 편의) — log.warn 출력
  - 토스 client-key 미설정 시 `upgradeComingSoon` 메시지 표시
  - `VITE_TOSS_CLIENT_KEY` 환경변수를 `.env.local`에 설정해야 결제 위젯 실제 동작
  - Free 제한 체크는 `PostService.createPost()`에서 — draft/scheduled/posted 무관하게 당월 생성 수 기준

### Phase 2-4: 랜딩 페이지 완성

- 구현 내용
  - `LandingPage.tsx` 전면 개편
    - **히어로 섹션:** 뱃지("무료로 시작"), 큰 헤드라인, 부제목, CTA 2개(시작/로그인), hint 텍스트
    - **앱 미리보기 mock:** 브라우저 프레임 CSS로 구현 — 캡션 생성 화면 시각화 (스크린샷 대체)
    - **3단계 흐름:** 아이콘·번호·화살표로 단계 연결, 설명 보강
    - **요금제 비교 카드:** Free / Pro 나란히, Pro 카드 강조 테두리 + "추천" 배지
    - **타깃 업종 확장:** 카페·식당·베이커리 + 뷰티/네일·헬스/운동 추가
    - **푸터:** 브랜드명 + 이용약관·개인정보처리방침·문의 링크, 반응형
  - `styles/index.css` — 랜딩 CSS 전면 교체 (sticky 헤더, clamp 반응형 h1, mock browser, 요금제 카드, 반응형 640px)
  - CSS 버그 수정: `--accent` → `--color-primary`, `--border` → `--border-color` (CaptionPage optional fields)
  - `locales/ko.json` / `locales/en.json` — `landing.*` 대폭 추가 (badge, howTitle, mockGenerate, mockCaptionText, audience 2종, pricing.*)
- 특이사항
  - 실제 스크린샷 없음 → CSS-only mock browser로 앱 UI 시각화 (배포 후 실제 스크린샷으로 교체 예정)
  - 요금제 카드 기능 목록은 `plan.free.features` / `plan.pro.features` i18n 배열 재사용
  - 헤더 `position: sticky` 적용 — 스크롤 시 메뉴 항상 표시

### Phase 2-3: AI 캡션 입력 고도화

- 구현 내용
  - **백엔드:**
    - `User.java` — `business_type`, `mood` 컬럼 추가 (JPA ddl-auto=update로 자동 반영)
    - `CaptionRequest.java` — `special_menu`, `event_promotion`, `keywords` 필드 추가 (nullable)
    - `GeminiService.java` — `generateCaption()` 시그니처 확장, 선택 입력값 프롬프트에 조건부 포함
    - `PostController.java` — `/caption` 엔드포인트에 `@AuthenticationPrincipal` 추가, 업종/분위기 저장 + 새 필드 전달
    - `UserService.java` — `updateBusinessProfile(id, businessType, mood)` 추가
    - `MeResponse.java` — `business_type`, `mood` 응답 포함
  - **프론트엔드:**
    - `types/index.ts` — `User` 타입에 `business_type?`, `mood?`, `plan?` 추가
    - `api/users.ts` — `getMe()` 추가
    - `api/posts.ts` — `generateCaption()` 파라미터를 객체로 변경, 선택 필드 추가
    - `CaptionPage.tsx` — 마운트 시 `GET /api/users/me` 로 업종/분위기 초기값 자동 로드, "추가 정보 입력" 토글 섹션 + 3개 입력 필드 (특별 메뉴, 이벤트, 키워드)
    - `styles/index.css` — `.caption-optional-toggle`, `.caption-optional-fields` 스타일 추가
    - i18n — `caption.optionalToggle`, `specialMenuLabel`, `eventPromotionLabel`, `keywordsLabel` 등 한/영 추가
- 특이사항
  - Gemini 프롬프트: 선택 입력이 없으면 해당 줄 생략 (null 체크)
  - 업종/분위기는 캡션 생성 버튼 클릭 시 자동 저장 → 다음 방문 시 드롭다운 자동 선택
  - `User.business_type`, `User.mood` 컬럼은 JPA `ddl-auto=update`로 자동 추가 (별도 마이그레이션 불필요)

### Phase 2-2: 예약 실패 이메일 알림

- 구현 내용
  - `build.gradle` — `spring-boot-starter-mail` 의존성 추가
  - `email/EmailService.java` 신규 생성
    - `JavaMailSender` 기반 HTML 이메일 발송
    - `@Async` — 스케줄러 스레드 블로킹 없이 별도 스레드에서 발송
    - 이메일 내용: 실패 게시물 썸네일, 캡션 미리보기, 히스토리 링크 버튼
  - `TaedibearStudioApplication.java` — `@EnableAsync` 추가
  - `schedule/ScheduledPublishJob.java` 재시도 로직 수정
    - 기존 버그: 1회 실패 시 즉시 `status=failed` → 재시도 불가
    - 수정: `retryCount` 증가 후 `< MAX_RETRY_COUNT(3)` 이면 `status=pending` 유지 (다음 cron 재처리)
    - 3회 실패 시: `status=failed`, `post.status=failed`, `EmailService.sendUploadFailureNotification()` 호출
    - `UserRepository`, `EmailService` 의존성 주입 추가
  - `application.yml` — `spring.mail.*` 설정 추가 (host/port/username/password/STARTTLS)
  - `application.yml` — `app.mail.from` 설정 추가
- 특이사항
  - 메일 서버 미설정 시 (`MAIL_USERNAME` 환경변수 없음) 발송 시도 시 `MessagingException` → EmailService에서 catch 후 log.error 처리 (앱 전체에는 영향 없음)
  - Gmail SMTP 기준 설정 (port 587, STARTTLS) — 다른 SMTP 사용 시 `MAIL_HOST`, `MAIL_PORT` 환경변수로 변경
  - Gmail 앱 비밀번호 발급 필요 (2단계 인증 활성화 → https://myaccount.google.com/apppasswords)

### Phase 1-3: 분석 대시보드 (`/analytics`)
- 구현 내용
  - **백엔드:** `AnalyticsController`, `AnalyticsService`, `AnalyticsSummaryResponse` 신규 생성
  - `GET /api/analytics/summary` API 추가 — 인증 필요, 자체 DB만 사용
  - `PostRepository` 4개 JPQL 쿼리 추가 (월별/요일별/상태별/이번달 집계)
  - `ScheduledPostRepository` `countScheduledPostsByUserId` 추가 (distinct postId)
  - **프론트엔드:** `AnalyticsPage.tsx` 신규 생성 (P-13), recharts 기반
  - 차트 5종: 월별 bar, 요일별 bar, 성공률 pie, 예약비율 pie, 이번 달 stat 카드 3개
  - `frontend/src/api/analytics.ts` 신규 생성 (`AnalyticsSummary` 타입 + `getAnalyticsSummary()`)
  - `App.tsx`: `/analytics` 라우트 추가
  - `NavBar.tsx`: "분석" 메뉴 링크 추가
  - CSS: `.analytics-*` 클래스군 (2열 그리드, 반응형 680px 단일 컬럼)
  - 다국어: `analytics.*` 번역 키 (ko/en)
  - 문서: `02` v1.4 (F-17), `03` v1.4 (P-13), `05` v1.3 (GET /api/analytics/summary), `07` 업데이트
- 특이사항
  - recharts 별도 설치 필요: `cd frontend && npm install recharts`
  - 다크모드 pie 색상: `document.documentElement.getAttribute('data-theme')` 으로 동적 전환
  - 월별 빈 달 처리: LinkedHashMap으로 12개월 슬롯 선생성 후 DB 결과 덮어쓰기
  - MySQL `FUNCTION('DAYOFWEEK', ...)` 사용 — JPQL 표준 외 함수 (H2 테스트 시 주의)

---

## 2026-07-01 (계속)

---

## 2026-07-02

### Phase 2-1: 크레딧 시스템

- 구현 내용
  - **백엔드:**
    - `User.java` — `credits`(기본 3, `@Builder.Default`), `adWatchCount`, `lastAdWatchDate` 필드 추가
    - `UserService.java` — `completeOnboarding(+2)`, `watchAd(+1, 하루 2회 제한)`, `deductCreditForCaption(-1, Pro 면제)` 메서드 추가
    - `UserController.java` — `POST /api/users/me/onboarding-complete`, `POST /api/users/me/credits/ad-watch` 엔드포인트 추가
    - `MeResponse.java` — `credits` 필드 포함
    - `PostController.java` — 캡션 생성 전 `deductCreditForCaption()` 호출 (Free 플랜 크레딧 0이면 403)
  - **프론트엔드:**
    - `types/index.ts` — `User` 인터페이스에 `credits?` 추가
    - `api/users.ts` — `completeOnboarding()`, `watchAd()` 추가
    - `OnboardingModal.tsx` — 완료 시 `completeOnboarding()` 호출 (+2 크레딧)
    - `CaptionPage.tsx` — 크레딧 상태 표시 바, "광고 보고 충전" 버튼, 크레딧 0이면 캡션 생성 버튼 비활성화, `handleWatchAd()` (2초 딜레이 시뮬레이션)
    - `DashboardPage.tsx` — Free 플랜 전용 크레딧 요약 카드 (0개면 경고 색상)
    - `styles/index.css` — `.summary-hint`, `.credit-status`, `.credit-badge`, `.credit-ad-btn` 추가
    - `locales/ko.json` / `en.json` — `dashboard.credits`, `dashboard.creditsEmpty`, `caption.credits`, `caption.watchAd`, `caption.adWatching`, `caption.err.adFailed` 번역 키 추가
- 특이사항
  - 광고 실제 연동 전: 2초 setTimeout으로 시뮬레이션 (Phase 3-1 AdSense 연동 시 교체)
  - Pro 플랜은 크레딧 차감 없음 (`deductCreditForCaption`에서 plan 체크 후 return)

### Phase 2-3: 관리자 이메일 알림 (`AdminReportScheduler`)

- 구현 내용
  - `EmailService.java` — `sendAdminDailyReport()` 메서드 추가 (`@Async`)
    - 실패 건수 / 재시도 성공 / 최종 실패 3가지 수치를 HTML 테이블로 발송
    - `@Value("${app.mail.admin:}")` 로 수신자 관리
  - `AdminReportScheduler.java` 신규 생성
    - `@Scheduled(cron = "0 0 9 * * *")` — 매일 오전 9시 실행
    - 어제 하루의 실패 현황 집계 후 totalFailed == 0이면 미발송
  - `ScheduledPostRepository.java` — `countByStatusAndScheduledAtBetween()` 추가
  - `PostRepository.java` — `countByStatusAndPostedAtBetween()` 추가
  - `application.yml` — `app.mail.admin: ${ADMIN_EMAIL:}` 추가
- 특이사항
  - `ADMIN_EMAIL` 환경변수 미설정 시 발송 스킵 (빈 문자열 수신자로 예외 발생 — EmailService에서 catch)
  - 집계 기준: `scheduled_posts.status=failed` (최종 실패) + `posts.status=posted` (재시도 성공)으로 "어제 실패했던 총 건수" 역산

### Phase 2-4: "이 스타일로 다시 생성" 버튼

- 구현 내용
  - `HistoryPage.tsx` — `posted` 상태 게시물 카드에 버튼 추가
    - 클릭 시 `navigate('/upload/caption', { state: { imageUrl, instagramAccountId } })` 로 CaptionPage로 이동
    - CaptionPage는 기존 `useEffect`에서 `getMe()`로 업종/분위기 자동 로드 → 이전 게시물과 동일 스타일로 생성 가능
  - `locales/ko.json` / `en.json` — `history.regenerate` 번역 키 추가
- 특이사항
  - 새 백엔드 API 없음 — CaptionPage 기존 상태 로드 로직 재사용
  - 이미지는 S3 URL 그대로 재사용 (재업로드 없음)

### Phase 2-5: 이용약관 / 개인정보처리방침 페이지

- 구현 내용
  - `TermsPage.tsx` 신규 생성 (`/terms`)
    - 8개 조항 (목적, 정의, 효력, 서비스 제공, 이용자 의무, 크레딧/결제, 면책, 준거법)
  - `PrivacyPage.tsx` 신규 생성 (`/privacy`)
    - 8개 항목 (수집 항목, 이용 목적, 보유 기간, 제3자 제공, 처리 위탁, 이용자 권리, 쿠키, 보호책임자)
  - `App.tsx` — `/terms`, `/privacy` 라우트 추가 (인증 불필요)
  - `LandingPage.tsx` — 푸터 `<a href>` → `<Link to>` 변경 (SPA 라우팅)
  - `styles/index.css` — `.legal-page`, `.legal-date`, `.legal-back`, `.legal-back-bottom` 스타일 추가 (다크모드 CSS 변수 적용)
- 특이사항
  - 법적 효력 있는 내용이 아닌 서비스 론칭용 초안 — 실제 배포 전 법률 검토 권장
  - 위탁사: AWS S3, Google Gemini API, 토스페이먼츠 명시

### Phase 2-6: 에러 핸들링 + 로딩 상태 전체 점검
- 구현 내용
  - **`frontend/src/api/client.ts`** — axios 인터셉터 추가
    - 네트워크 오류 (`!error.response`) 시 `error.isNetworkError = true` 플래그 세팅
    - 401 + 토큰 보유 상태 → `localStorage` 토큰 제거 + `sessionStorage.session_expired = '1'` 세팅 후 `/login` 리다이렉트
  - **`pages/LoginPage.tsx`** — 세션 만료 배너
    - mount 시 `sessionStorage.getItem('session_expired')` 확인 후 제거, `banner-warn` 클래스 배너 표시
  - **`pages/DashboardPage.tsx`** — `Promise.all` 에러 catch 추가 (네트워크/서버 오류 Toast 표시)
  - **`pages/SettingsPage.tsx`** — `listInstagramAccounts()` 에러 catch 추가
  - **`pages/CalendarPage.tsx`** — 에러 메시지 i18n 키 수정 + 빈 상태 개선
    - 잘못된 키(`t('common.loading')`) → `t('error.loadFailed')` 수정
    - 전체 예약 없을 때 `EmptyState` 컴포넌트로 교체 (emoji 📅 + "첫 게시물 예약하기" 버튼)
  - **`pages/AnalyticsPage.tsx`** — 에러 핸들링 + 빈 상태 개선
    - `.catch()` 잘못된 키(`t('common.loading')`) → 네트워크/서버 오류 분기로 교체
    - 데이터 없을 때 `<p className="muted">` → `EmptyState` 컴포넌트 (emoji 📊)
  - **`components/EmptyState.tsx`** — `emoji` prop 추가 (48px 블록 이모지)
  - **`styles/index.css`** — 스타일 2종 추가
    - `.banner-warn` / `[data-theme='dark'] .banner-warn` — 경고 배너 (노란 계열)
    - `.empty-state-emoji` — 48px 이모지 블록
  - **`locales/ko.json` / `en.json`** — `error.network`, `error.loadFailed`, `error.sessionExpired`, `calendar.empty`, `calendar.emptyAction` 번역 키 추가
- 특이사항
  - 네트워크 오류와 서버 오류를 axios 인터셉터에서 플래그(`isNetworkError`)로 구분 → 각 페이지 catch에서 일관 처리
  - 세션 만료 UX: `sessionStorage` 플래그로 리다이렉트 후 로그인 페이지에 배너 표시 (페이지 전환 후에도 상태 유지)

---

## 2026-07-02

### 전체 코드 보안/품질 점검 + 크레딧 버그 수정 (H1/H2/H3)
- 구현 내용
  - 전체 코드 보안/품질 점검 보고서 작성 → `docs/08_보안품질점검보고서.md` (Critical 8건, High 8건, Medium 12건 + 기능 제안)
  - 크레딧 관련 버그 3건 수정 (백엔드만, 프론트 수정 없음):
    - **`domain/User.java`** — `onboarding_completed`(온보딩 보너스 1회 플래그), `last_ad_watch_at`(광고 최소 간격 검증용) 컬럼 추가
    - **`repository/UserRepository.java`** — `findByIdForUpdate()`(PESSIMISTIC_WRITE 행 잠금), `deductOneCredit()`(원자적 크레딧 차감 UPDATE) 추가
    - **`user/UserService.java`** — `completeOnboarding` 1회 제한(멱등), `watchAd` 행 잠금 + 최소 간격 30초, `deductCreditForCaption` 원자적 차감으로 교체
    - **`payment/PaymentService.java`** — `checkPlanLimit`에 행 잠금 + `Propagation.MANDATORY` 적용 (무료 월 10회 제한 race 방지)
  - 학습 노트 작성 → `docs/09_크레딧버그수정_학습노트.md` (수정 전/후 코드 비교 + 트러블슈팅)
  - 트러블슈팅 [008]~[010] 기록
- 특이사항
  - **샌드박스에서 빌드 미검증** — Java 17/Maven Central 접근 불가로 컴파일 확인 못 함. 로컬에서 `./gradlew compileJava` 실행 필요
  - 새 컬럼 2개는 `ddl-auto: update`로 자동 반영되나, `onboarding_completed NOT NULL`은 기존 행 때문에 수동 ALTER가 필요할 수 있음 (학습 노트 T-4 참고)
  - 광고 시청 검증은 부분 수정 — 근본 해결은 광고 SDK SSV 콜백 (TODO 주석 남김)

---

## 2026-07-03

### 크레딧 버그 로컬 검증 완료 + C1 결제 금액 검증 추가
- 구현 내용
  - 로컬 빌드(`./gradlew compileJava`) 및 서버 기동 확인, 크레딧 버그 3건 API 검증 통과 (학습 노트 "검증 결과" 참고)
  - 서버 기동 이슈 2건 해결: `.env` 미로드로 인한 WeakKeyException(트러블슈팅 [011]), WebClient 빈 누락(트러블슈팅 [012])
  - **C1: 결제 금액 서버 검증** — `PaymentService.confirm`에 `PRO_PLAN_PRICE`(29,000원) 상수 추가, 토스 승인 API 호출 전 `request.amount()` 대조. 불일치 시 400 + 경고 로그 (보안 보고서 08 C1)
- 특이사항
  - 가격 상수는 프론트 `PlanSection.tsx`의 `amount: 29000`과 반드시 동기화 유지 — 가격 변경 시 양쪽 모두 수정 필요
  - C1 검증: 서버 재시작 후 amount=100 요청 → `"결제 금액이 올바르지 않아요."` 확인 (재시작 전 구코드로 첫 검증이 실패해 가짜 결제 1건 수동 삭제함)

### C2: 토스 키 미설정 시 결제 승인 스킵 차단
- 구현 내용
  - `app.toss.dev-mode`(기본 false, `TOSS_DEV_MODE` 환경변수) 설정 추가
  - `callTossConfirm`/`callTossCancel`: 시크릿 키 미설정 시 dev-mode가 켜져 있을 때만 스킵, 아니면 500 에러로 결제 실패 처리 — 운영에서 키 누락 시 "승인 없이 Pro 부여" 원천 차단
- 특이사항
  - 로컬에서 결제 플로우 테스트하려면 `.env`에 `TOSS_DEV_MODE=true` 추가 필요
  - C2 검증: 키 미설정 + 정상 금액(29,000) 요청 → `"결제 설정에 문제가 있어요."` 확인

### C3: CORS 허용 오리진 제한
- 구현 내용
  - `SecurityConfig.corsConfigurationSource`: `allowedOriginPatterns("*")` → `setAllowedOrigins(List.of(clientUrl))` (`app.client-url` 주입)
  - 와일드카드 + `allowCredentials(true)` 조합 제거 — 임의 사이트에서의 인증 요청 위조 차단
- 특이사항
  - 운영 배포 시 `CLIENT_URL` 환경변수를 실제 프론트 도메인으로 설정해야 CORS가 정상 동작
  - 남은 Critical: C4~C6(토큰 체계), C7(SSRF), C8(JWT 기본값)
