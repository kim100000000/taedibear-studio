# Taedibear Studio — 개발일지

**작성자:** @taedibear  
**최초 작성:** 2026-07-01

---

## 2026-06-27

### 프로젝트 초기 세팅 완료
- 구현 내용
  - 프로젝트 폴더 구조 생성 (`frontend/`, `backend/`, `docs/`)
  - `.gitignore`, `README.md` 작성
  - 요구사항 명세서 (`00_요구사항명세서.md`), 서비스 기획서 초안 작성
- 특이사항
  - MVP 범위 확정: 회원가입/로그인, 이미지 업로드, AI 캡션, 예약 업로드, 히스토리

### backend-node 백엔드 구현 (Node.js 시절)
- 구현 내용
  - Express 기반 MVC 구조 세팅
  - MySQL 연결, 공통 에러/응답 핸들러
  - User/Post/InstagramAccount/ScheduledPost 모델 정의
  - 회원가입/로그인 API (JWT, bcrypt)
  - 구글/네이버 소셜 로그인 (Passport.js)
  - 사용자 정보 API (`GET /api/users/me`)
  - 이미지 업로드 API (AWS S3)
  - AI 캡션 생성 API (Gemini `gemini-2.5-flash`)
  - 게시물 CRUD API
  - Meta API 연동 및 즉시 업로드
  - 예약 업로드 cron job (node-cron)
- 특이사항
  - Gemini 모델: 최초 `gemini-1.5` → `gemini-2.5-flash` 수정 (deprecated 대응)

---

## 2026-07-01

### Spring Boot 백엔드 전환
- 구현 내용
  - Node.js 백엔드 전체를 Spring Boot 3.x (Java 17)으로 재구현
  - Spring Security + JWT 인증
  - JPA 엔티티 (`User`, `Post`, `InstagramAccount`, `ScheduledPost`)
  - Google / Kakao / Naver OAuth (Spring WebClient 직접 호출, `NaverOAuthClient.java`)
  - 예약 발행 스케줄러: `@Scheduled(cron = "0 * * * * *")` — 1분 폴링
  - 개발 포트 4000 → 8080 변경
- 특이사항
  - Naver OAuth는 Spring Security OAuth2 미지원으로 `NaverOAuthClient.java` 직접 구현
  - `GET /api/instagram/connect` 인증: 브라우저 리다이렉트 특성상 `?token=<JWT>` 쿼리 파라미터 방식

### React TypeScript 전환 + 부가 기능
- 구현 내용
  - 기존 JavaScript React → TypeScript(strict) 전환
  - `axios` 401 인터셉터 추가 (세션 만료 자동 로그아웃)
  - `ErrorBoundary` 컴포넌트 추가
  - `ProtectedRoute` 로딩 상태 통일
  - 다크모드: CSS 커스텀 변수 (`:root` / `[data-theme='dark']`), `ThemeContext`, `ThemeToggle`
  - 플랜 UI: `PlanSection` 컴포넌트 (무료/프로 비교 카드)
  - 온보딩: `OnboardingModal` (4단계 슬라이드, 첫 로그인 1회 노출)
- 특이사항
  - 다크모드 초기값: `localStorage('theme')` → `prefers-color-scheme` 순으로 결정
  - 플랜 상태는 현재 `localStorage` 임시 저장 (결제 연동 전)

### i18n 다국어 지원 (한국어 / 영어)
- 구현 내용
  - `i18next` + `react-i18next` 설치 및 초기 설정
  - 번역 파일: `src/locales/ko.json`, `src/locales/en.json`
  - 모든 UI 문자열 `t()` 로 교체
  - `LangToggle` 버튼 (NavBar + 랜딩 헤더)
  - 언어 설정 `localStorage('lang')` 유지
- 특이사항
  - 배열 번역은 `{ returnObjects: true }` 옵션 사용 (온보딩 steps 등)
  - API로 전달하는 값(업종, 분위기)은 한국어 그대로 유지 (Gemini 프롬프트 호환)

### 기존 문서 동기화
- 구현 내용
  - `00_요구사항명세서.md` v1.2: Spring Boot 시스템 다이어그램, FR-111 네이버 로그인, 3.6 UI/UX 요구사항 추가
  - `02_기능명세서.md` v1.1: F-02 네이버, F-07 Spring @Scheduled, F-11~F-14 추가
  - `03_화면설계서.md` v1.1: 네이버 버튼, NavBar 토글, PlanSection, 컴포넌트 목록 갱신
  - `04_DB설계서.md` v1.1: cron → Spring @Scheduled 레퍼런스 수정
  - `05_API명세서.md` v1.2: 포트 8080, NaverOAuthClient 구현 방식, instagram/connect 인증 방식 명시
- 특이사항
  - 문서와 실제 코드 간 괴리가 커서 전체 재동기화 수행

### Phase 1-1: 콘텐츠 캘린더 (`/calendar`)
- 구현 내용
  - `CalendarPage.tsx` 신규 생성 (P-11)
  - 월간 캘린더 뷰 (7열 CSS Grid, 이전/다음 월 이동)
  - 날짜별 예약 게시물 썸네일 + 시간 표시
  - 상태별 색상 구분: 예약 중(파랑) / 완료(초록) / 실패(빨강)
  - 드래그&드롭 예약 시간 변경 (`PUT /api/scheduled/:id` 재활용)
    - `pending` 상태만 드래그 가능
    - 드롭 시 원본 HH:MM 유지, 날짜만 변경
    - 10분 이전 드롭 차단
  - 클릭 시 상세 모달 (이미지, 캡션, 상태 배지, 시간, 예약 취소)
  - NavBar에 "캘린더" 메뉴 추가
  - CSS: `--cal-*` 테마 변수 (라이트/다크)
  - 다국어: `calendar.*` 번역 키
  - 문서: `02` v1.2 (F-15), `03` v1.2 (P-11, 흐름도 업데이트)
- 특이사항
  - 새 백엔드 API 없음 — 기존 `GET /api/scheduled`로 충분 (post 정보 포함 반환)
  - `statusBadgeClass()`: ScheduleStatus → PostStatus 클래스 매핑으로 기존 CSS 재사용

### Phase 1-2: AI 이미지 편집 (`/upload/edit`)
- 구현 내용
  - `ImageEditorPage.tsx` 신규 생성 (P-12)
  - Canvas API 기반 실시간 편집 미리보기
  - 비율 크롭 4종: 원본 / 1:1 / 4:5 / 1.91:1 (중앙 기준 자동 계산)
  - 필터 프리셋 5종: 감성 / 밝음 / 빈티지 / 선명 / 따뜻함 (brightness/contrast/saturation 조합)
  - 슬라이더 3종: 밝기 / 대비 / 채도 (범위 0~200, 기본 100)
  - 편집 초기화 버튼
  - 내보내기: `canvas.toBlob()` → JPEG 92% → `POST /api/posts/upload` → CaptionPage
  - `UploadPage.tsx` 수정: S3 직접 업로드 제거, `/upload/edit`으로 이동
  - CSS: 에디터 칩 버튼, 슬라이더, 반응형 레이아웃 추가
  - 다국어: `editor.*` 번역 키
  - 문서: `00` v1.3 (FR-605~608), `02` v1.3 (F-16), `03` v1.3 (P-12, 흐름도)
- 특이사항
  - 새 백엔드 API 없음 — 브라우저 Canvas에서 모든 편집 처리 후 기존 업로드 API 사용
  - 업로드 플로우 변경: UploadPage → **ImageEditorPage** → CaptionPage
  - `canvas.filter = "brightness(B%) contrast(C%) saturate(S%)"` 방식 (CSS filter 활용)
  - 커밋 시 `.git/HEAD.lock` / `index.lock` 이슈 발생 → 수동 커밋 필요 (macOS git maintenance 프로세스 충돌)

### Phase 1-3: 분석 대시보드 (`/analytics`)
- 구현 내용
  - **백엔드:** `AnalyticsController`, `AnalyticsService`, `AnalyticsSummaryResponse` 신규 생성
  - `GET /api/analytics/summary` API 추가 — 인증 필요, 자체 DB만 사용
  - `PostRepository` 4개 JPQL 쿼리 추가 (월별/요일별/상태별/이번달 집계)
  - `ScheduledPostRepository` `countScheduledPostsByUserId` 추가 (distinct postId)
  - **프론트엔드:** `AnalyticsPage.tsx` 신규 생성 (P-13), recharts 기반
  - 차트 5종: 월별 bar, 요일별 bar, 성공률 pie, 예약비율 pie, 이번 달 stat 카드 3개
  - `frontend/src/api/analytics.ts` 신규 생성 (`AnalyticsSummary` 타입 + `getAnalyticsSummary()`)
  - `App.tsx`: `/analytics` 라우트 추가
  - `NavBar.tsx`: "분석" 메뉴 링크 추가
  - CSS: `.analytics-*` 클래스군 (2열 그리드, 반응형 680px 단일 컬럼)
  - 다국어: `analytics.*` 번역 키 (ko/en)
  - 문서: `02` v1.4 (F-17), `03` v1.4 (P-13), `05` v1.3 (GET /api/analytics/summary), `07` 업데이트
- 특이사항
  - recharts 별도 설치 필요: `cd frontend && npm install recharts`
  - 다크모드 pie 색상: `document.documentElement.getAttribute('data-theme')` 으로 동적 전환
  - 월별 빈 달 처리: LinkedHashMap으로 12개월 슬롯 선생성 후 DB 결과 덮어쓰기
  - MySQL `FUNCTION('DAYOFWEEK', ...)` 사용 — JPQL 표준 외 함수 (H2 테스트 시 주의)
