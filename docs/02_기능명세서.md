# Taedibear Studio — 기능 명세서

**버전:** v1.1  
**작성일:** 2026-06-27  
**범위:** MVP 기능

---

## 1. 기능 목록 요약

| 기능 ID | 기능명 | 우선순위 | 상태 |
|---------|--------|----------|------|
| F-01 | 회원가입 / 로그인 | 필수 | MVP |
| F-02 | 소셜 로그인 (구글, 카카오, 네이버) | 필수 | MVP |
| F-03 | 인스타그램 계정 연동 | 필수 | MVP |
| F-04 | 사진 업로드 | 필수 | MVP |
| F-05 | AI 캡션 + 해시태그 생성 | 필수 | MVP |
| F-06 | 즉시 인스타 업로드 | 필수 | MVP |
| F-07 | 예약 업로드 | 필수 | MVP |
| F-08 | 업로드 히스토리 | 필수 | MVP |
| F-09 | 분석 대시보드 | 선택 | 추후 |
| F-10 | 멀티 계정 관리 | 선택 | 추후 |
| F-11 | 다크모드 | 필수 | MVP |
| F-12 | 다국어 지원 (한국어 / 영어) | 필수 | MVP |
| F-13 | 플랜 선택 UI (무료 / 프로) | 필수 | MVP |
| F-14 | 온보딩 플로우 | 필수 | MVP |

---

## 2. 기능 상세

### F-01. 회원가입 / 로그인

**설명:** 이메일과 비밀번호로 계정을 생성하고 로그인합니다.

| 항목 | 내용 |
|------|------|
| 입력값 | 이름, 이메일, 비밀번호 (8자 이상) |
| 유효성 검사 | 이메일 형식, 비밀번호 강도, 중복 이메일 확인 |
| 보안 | bcrypt로 비밀번호 암호화, JWT 토큰 발급 |
| 토큰 유효기간 | 7일 |

**예외 처리:**
- 중복 이메일: "이미 사용 중인 이메일이에요."
- 잘못된 비밀번호: "이메일 또는 비밀번호가 올바르지 않아요."

---

### F-02. 소셜 로그인

**설명:** 구글, 카카오, 네이버 계정으로 간편 로그인합니다.

| 항목 | 내용 |
|------|------|
| 지원 플랫폼 | Google OAuth 2.0, Kakao REST API, Naver REST API |
| 신규 사용자 | 자동으로 계정 생성 후 로그인 |
| 기존 사용자 | 소셜 ID로 계정 조회 후 로그인 |
| 토큰 발급 | 로그인 성공 시 JWT 발급 후 대시보드 이동 |
| 이메일 미제공 시 | `kakao_<id>@kakao.taedibear.local` / `naver_<id>@naver.taedibear.local` 형식으로 내부 이메일 생성 |
| 구현 방식 | Spring Boot — GoogleOAuthClient, KakaoOAuthClient, NaverOAuthClient (WebClient 직접 호출) |

---

### F-03. 인스타그램 계정 연동

**설명:** Meta Graph API를 통해 사용자의 인스타그램 계정을 연동합니다.

| 항목 | 내용 |
|------|------|
| 방식 | Meta OAuth 2.0 |
| 필요 권한 | `instagram_basic`, `instagram_content_publish` |
| 저장 정보 | instagram_user_id, username, access_token, 만료 시간 |
| 토큰 갱신 | 만료 7일 전 자동 갱신 |

**제약:**
- 인스타그램 비즈니스 계정 또는 크리에이터 계정만 API 사용 가능
- 개인 계정은 연동 불가 (안내 메시지 표시)

---

### F-04. 사진 업로드

**설명:** 사용자가 가게 사진을 업로드합니다.

| 항목 | 내용 |
|------|------|
| 지원 형식 | JPG, PNG, WEBP |
| 최대 용량 | 10MB |
| 저장 방식 | AWS S3 (또는 Cloudinary) |
| 업로드 방식 | 드래그&드롭, 파일 선택, 모바일 카메라 촬영 |
| 미리보기 | 업로드 전 이미지 미리보기 제공 |

**인스타그램 이미지 규격:**
- 정사각형: 1080 x 1080px 권장
- 가로형: 1080 x 566px
- 세로형: 1080 x 1350px

---

### F-05. AI 캡션 + 해시태그 생성

**설명:** Google Gemini API를 통해 업종에 맞는 캡션과 해시태그를 자동 생성합니다.

| 항목 | 내용 |
|------|------|
| 사용 API | Google Gemini (gemini-2.5-flash) |
| 입력값 | 이미지 URL, 업종, 분위기 (선택) |
| 출력값 | 캡션 (150자 내외), 해시태그 (10~15개) |
| 수정 기능 | 생성된 캡션 직접 수정 가능 |
| 재생성 | 마음에 안 들면 재생성 가능 (최대 3회) |

**프롬프트 전략:**
- 업종 정보를 포함하여 맞춤형 캡션 생성
- 감성적이고 자연스러운 한국어 문체 적용
- 인기 해시태그 포함 (업종 + 지역 + 감성 태그 조합)

---

### F-06. 즉시 인스타 업로드

**설명:** 작성된 게시물을 바로 인스타그램에 업로드합니다.

| 항목 | 내용 |
|------|------|
| 사용 API | Meta Graph API - Content Publishing |
| 업로드 흐름 | 이미지 컨테이너 생성 → 게시물 발행 |
| 소요 시간 | 약 3~10초 |
| 성공 시 | 인스타 게시물 ID 저장, 히스토리에 기록 |
| 실패 시 | 오류 메시지 표시 + 재시도 버튼 |

---

### F-07. 예약 업로드

**설명:** 원하는 날짜와 시간에 자동으로 인스타그램에 업로드되도록 예약합니다.

| 항목 | 내용 |
|------|------|
| 최소 예약 시간 | 현재 시간 + 10분 이후 |
| 예약 변경 | 업로드 전까지 변경/취소 가능 |
| 자동 실행 | Spring @Scheduled(cron = "0 * * * * *") — 1분 단위 실행 |
| 실패 시 재시도 | 최대 3회 자동 재시도 |
| 알림 | 업로드 성공/실패 시 알림 (추후) |

---

### F-08. 업로드 히스토리

**설명:** 업로드된 게시물 목록을 확인하고 관리합니다.

| 항목 | 내용 |
|------|------|
| 표시 정보 | 이미지 썸네일, 캡션 미리보기, 상태, 업로드 시간 |
| 상태 종류 | 작성중 / 예약됨 / 업로드 완료 / 실패 |
| 정렬 | 최신순 (기본) |
| 기능 | 게시물 수정, 삭제, 인스타 링크 열기 |

---

### F-11. 다크모드

**설명:** 라이트/다크 테마를 전환합니다. 시스템 설정을 자동으로 감지하며 수동 전환도 지원합니다.

| 항목 | 내용 |
|------|------|
| 구현 방식 | CSS 커스텀 변수 (`:root` / `[data-theme='dark']`), `ThemeContext` |
| 초기값 | `localStorage('theme')` → `prefers-color-scheme` 순으로 결정 |
| 유지 | localStorage 'theme' 키에 저장, 새로고침 후에도 유지 |
| 컴포넌트 | `ThemeToggle` 버튼 (🌙/☀️) — NavBar 및 랜딩 페이지 헤더에 노출 |

---

### F-12. 다국어 지원

**설명:** 한국어(기본)와 영어를 전환합니다.

| 항목 | 내용 |
|------|------|
| 지원 언어 | 한국어 (ko), 영어 (en) |
| 라이브러리 | i18next + react-i18next |
| 번역 파일 | `src/locales/ko.json`, `src/locales/en.json` |
| 초기값 | localStorage 'lang' 키 → 기본 'ko' |
| 컴포넌트 | `LangToggle` 버튼 (KO/EN) — NavBar 및 랜딩 페이지 헤더에 노출 |

---

### F-13. 플랜 선택 UI

**설명:** 무료/프로 플랜을 비교하고 현재 플랜을 확인합니다.

| 항목 | 내용 |
|------|------|
| 위치 | 설정 페이지 (P-10) 내 PlanSection 컴포넌트 |
| 플랜 종류 | free (무료) / pro (프로) |
| 현재 플랜 저장 | localStorage 'plan' 키 (TODO: 서버 연동 시 DB plan 필드로 교체) |
| 업그레이드 | 결제 연동 전 — 안내 토스트 표시 |

---

### F-14. 온보딩 플로우

**설명:** 신규 사용자의 첫 로그인 시 서비스 사용 방법을 4단계 모달로 안내합니다.

| 항목 | 내용 |
|------|------|
| 트리거 | 대시보드 첫 방문 시 (localStorage `onboarding_seen_<userId>` 키 기준) |
| 단계 수 | 4단계 (환영 → 사진 업로드 → 예약 설정 → 시작) |
| 컴포넌트 | `OnboardingModal` — 도트 인디케이터, 이전/다음/건너뛰기 버튼 |
| 노출 횟수 | 계정당 1회, 건너뛰어도 재노출 안 됨 |

---

## 3. 추후 추가 기능

| 기능 | 설명 |
|------|------|
| 분석 대시보드 | 팔로워 증가, 게시물 조회수, 좋아요 수 추적 |
| 멀티 계정 | 여러 인스타 계정 한 번에 관리 |
| 콘텐츠 캘린더 | 월간 업로드 일정 시각화 |
| AI 이미지 편집 | 필터, 밝기 자동 보정 |
| 리뷰 자동 답글 | 댓글에 AI 자동 답변 |

---

## 4. 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| v1.0 | 2026-06-27 | 최초 작성 | @taedibear |
| v1.1 | 2026-07-01 | F-02 네이버 소셜 로그인 추가 / F-07 cron → Spring @Scheduled 반영 / F-11~F-14 신규 기능 추가 (다크모드, 다국어, 플랜 UI, 온보딩) | @taedibear |
