# Taedibear Studio — 개발 + 비즈니스 체크리스트
**작성일:** 2026-07-01  
**GitHub:** github.com/kim100000000/taedibear-studio

---

## ✅ Phase 0 — 프로젝트 초기 세팅
- [x] 폴더 구조 생성 (frontend/, backend/, docs/)
- [x] .gitignore, README.md 작성
- [x] 요구사항 명세서 작성
- [x] 서비스 기획서 작성
- [x] GitHub 레포 생성 및 운영

---

## ✅ Phase 1 — MVP 핵심 기능
- [x] 회원가입 / 이메일 로그인 (JWT, bcrypt)
- [x] 소셜 로그인 (구글 / 카카오 / 네이버)
- [x] 사진 업로드 (AWS S3)
- [x] AI 캡션 + 해시태그 생성 (Gemini gemini-2.5-flash)
- [x] 예약 업로드 스케줄러 (Spring @Scheduled, 1분 폴링)
- [x] 업로드 히스토리
- [x] Node.js → Spring Boot 전환 (Java 17)
- [x] React → TypeScript 전환 (strict 모드)
- [x] 다크모드 (CSS 커스텀 변수, ThemeContext)
- [x] 온보딩 모달 (4단계 슬라이드, 첫 로그인 1회)
- [x] 다국어 지원 (한국어 / 영어, i18next)
- [x] 콘텐츠 캘린더 (드래그&드롭 예약 변경)
- [x] AI 이미지 편집 (Canvas API, 필터 5종, 크롭)
- [x] 분석 대시보드 (recharts, 월별/요일별/성공률)

---

## 🔴 Phase 2 — 수익화 + 배포 전 완성

### 2-1. 크레딧 시스템
- [x] DB credits 컬럼 추가
- [x] 가입 시 크레딧 3개 자동 지급
- [x] 온보딩 완료 시 +2개 지급 → 총 5개
- [x] 캡션 생성 시 크레딧 1개 차감
- [x] 크레딧 0이면 "광고 보고 충전" 버튼 표시
- [x] 광고 충전: 1회당 크레딧 1개, 하루 최대 2회
- [x] 대시보드에 남은 크레딧 표시
- [x] 문서 업데이트

### 2-2. 결제 연동 (토스페이먼츠)
- [x] payments 테이블 생성
- [x] Free 플랜 제한 (당월 10회 초과 시 403)
- [x] Pro 플랜: 무제한, 광고 없음
- [x] POST /api/payments/confirm
- [x] GET /api/payments/status
- [x] POST /api/payments/cancel
- [x] GET /api/users/me/usage
- [x] 대시보드 구독 상태 카드 추가
- [x] /settings 구독 관리 섹션
- [x] 한도 초과 시 Pro 업그레이드 유도 모달
- [x] 문서 업데이트
- [ ] 실제 키 발급 및 테스트 → `docs/TODO/토스결제.md` 참고

### 2-3. 이메일 알림 (JavaMailSender)
- [x] 메일 서버 연동 (spring-boot-starter-mail, Gmail SMTP)
- [x] 문서 업데이트
- [ ] 실제 SMTP 환경변수 설정 → `docs/TODO/이메일_설정.md` 참고

**고객용:**
- [x] 예약 업로드 3회 최종 실패 시 이메일 발송
- [x] 이메일 내용: 실패 게시물 썸네일 + 재시도 링크

**관리자용 (성태님):**
- [x] 매일 오전 9시 실패 현황 요약 이메일 발송 (`@Scheduled`)
- [x] 실패 0건이면 발송 안 함
- [x] 실패 1건 이상일 때만 발송
- [x] 내용: "실패 X건 / 재시도 성공 X건 / 최종 실패 X건"
- [x] 관리자 이메일 `.env`에서 관리 (`ADMIN_EMAIL` 환경변수)

### 2-4. AI 캡션 입력 고도화
- [x] 추가 입력 필드 (오늘의 메뉴, 이벤트, 키워드)
- [x] 톤앤매너(업종/분위기) DB 저장 → 다음 방문 시 자동 반영
- [x] 잘 됐던 게시물 "이 스타일로 다시 생성" 버튼
- [x] 문서 업데이트

### 2-5. 랜딩 페이지 완성
- [x] 히어로 섹션 (핵심 문구 + CTA)
- [x] 3단계 설명 (업로드 → AI 캡션 → 자동 업로드)
- [x] 요금제 카드 (Free vs Pro 비교)
- [ ] 실제 사용 화면 스크린샷 (현재 CSS mock으로 대체)
- [x] 이용약관 페이지
- [x] 개인정보처리방침 페이지
- [x] 푸터 (이용약관·개인정보·문의 링크)

### 2-6. 에러 핸들링 + 로딩 상태 점검
- [x] 모든 API 호출 로딩 스피너 확인
- [x] 네트워크 오류 안내 메시지 (axios 인터셉터 isNetworkError 플래그)
- [x] 빈 상태 화면 (캘린더, 분석 — EmptyState 컴포넌트)
- [x] 401 만료 시 자동 로그아웃 안내 (sessionStorage 배너)

### 2-7. 배포 환경 설정
- [x] 환경변수 dev/prod 분리 (application.yml — 모든 설정값을 `${VAR:기본값}` 환경변수로 분리, 로컬 기본값 + 운영 Railway 환경변수)
- [x] CORS 운영 도메인 설정 (SecurityConfig — `app.client-url` 단일 오리진만 허용, 와일드카드 금지)
- [x] Vercel 프론트엔드 배포 (taedibear-studio.vercel.app 운영 중 확인)
- [x] Railway 백엔드 배포 (taedibear-studio-production.up.railway.app 운영 중 확인, nixpacks.toml로 backend-java 빌드)
- [ ] 도메인 연결 (선택)

---

## 🟡 Phase 3 — 배포 후

### 3-1. 광고 연동
- [ ] Google AdSense 계정 신청
- [ ] 사이트 승인 (1~2주 소요)
- [ ] 광고 위치 설정
  - [ ] 히스토리 페이지 하단
  - [ ] 캡션 생성 완료 후 대기 화면
  - [ ] 대시보드 사이드
- [ ] "광고 보고 충전" 버튼에 실제 광고 연결
- [ ] 하루 2회 제한 로직 확인

### 3-2. 유저 확보
- [ ] 동네 카페 직접 방문 영업 (무료 체험 제안)
- [ ] @taedibear 인스타 계정 개설
- [ ] 빌드인퍼블릭 콘텐츠 시작 (만드는 과정 공유)
- [ ] 네이버 카페 자영업자 커뮤니티 베타 모집 글
- [ ] 인스타 연동 가이드라인 제작

### 3-3. Meta API 연결
- [x] 개발자 계정 인증 재시도 (2026-07-06: "Invalid Scopes" 오류 해결 — 앱이 "Instagram API with Instagram Login"으로 설정돼 있어 코드가 요청하는 구버전 스코프가 거부됨. "Facebook 로그인이 포함된 API 설정 > 권한 및 기능"에서 5개 권한 추가 후 해결. 상세: `06_트러블슈팅.md` 추가 예정)
- [x] 기존 코드 연결 및 테스트 (실제 Instagram 계정 1개 연동 성공 확인)
- [x] 즉시 업로드 실제 동작 확인 — **버그 발견 및 수정**: `CaptionPage.tsx`가 게시물을 draft로 저장(`createPost`)만 하고 실제 발행 API(`publishPost`)를 호출하지 않아, UI는 "업로드 완료" 성공 화면을 보여주지만 실제로는 인스타그램에 게시되지 않던 문제. `publishPost` 호출을 추가해 수정 (frontend `tsc` 타입체크 통과). ⚠️ 배포 후 실제 게시 여부 재확인 필요
- [ ] 예약 업로드 실제 동작 확인
- [ ] Meta 앱 심사 제출 (현재 Standard Access — 관리자/개발자/테스터 계정만 사용 가능, 일반 사용자 확대 시 App Review 필요)

---

## 🟢 Phase 4 — Meta API 연결 후

### 4-1. 멀티 계정 관리
- [x] 여러 인스타 계정 연동 UI (설정 페이지 — 계정 목록/추가 연동/해제, 백엔드는 이미 계정별 upsert로 다중 계정 지원)
- [x] 게시물 작성 시 계정 선택 (업로드 페이지 — 계정 선택 드롭다운)
- [x] 계정별 히스토리/통계 분리 (2026-07-06 신규: 히스토리·분석 API에 `instagram_account_id` 필터 추가, 프론트에 계정 선택 드롭다운 추가. frontend `tsc` 통과, 백엔드는 이 환경에 JDK17/Gradle이 없어 실제 컴파일 검증 못함 — 배포 전 `./gradlew build`로 직접 확인 필요)

### 4-2. 분석 대시보드 고도화 (2026-07-06 신규 구현 — 아래 항목 전부 동일한 주의사항 적용)
- [ ] 팔로워 증가 추이 — 코드 작성 완료 (`FollowerSnapshotScheduler` 매일 새벽 3시 스냅샷 저장 + 분석 대시보드 라인 차트). **미검증**: `instagram_manage_insights` 권한을 Meta 앱에 추가하고, 계정을 재연동해야 실제로 값이 들어옴
- [ ] 게시물 조회수 / 좋아요 / 저장 수 — 코드 작성 완료 (히스토리에 "인사이트 보기" 버튼, `GET /api/posts/:id/insights`). 위와 동일하게 권한 추가 + 재연동 필요
- [ ] 게시물별 인게이지먼트율 — engagement 메트릭으로 포함
- [ ] 자체 데이터와 통합 — 히스토리/분석 화면에 함께 표시되도록 구현

### 4-3. 리뷰 자동 답글 (2026-07-06 신규 구현)
- [ ] Meta Graph API 댓글 조회 — 코드 작성 완료 (`ReviewService`가 최근 게시물 5개의 댓글을 가져와 `review_comments` 테이블에 동기화). **미검증**: `instagram_manage_comments` 권한 추가 + 재연동 필요
- [ ] Gemini 답글 자동 생성 — 코드 작성 완료 (`GeminiService.generateCommentReply`, 댓글 동기화 시 자동으로 초안 생성)
- [ ] 사용자 승인 후 발행 or 자동 발행 선택 — 코드 작성 완료 (계정별 토글: 설정 페이지, `POST /reply`로 승인 발행, `AutoReplyScheduler` 30분마다 자동 발행 계정 처리). 배포 전 실제 댓글로 테스트 필요

**Phase 4 공통 주의사항**: 이 코드는 이 세션에서 작성됐고 프론트엔드는 `tsc` 타입체크를 통과했지만, 백엔드(Java)는 이 작업 환경에 JDK 17과 Gradle/Maven 저장소 접근이 막혀 있어 실제 컴파일을 확인하지 못했다. 배포 전 반드시 로컬 또는 CI에서 `./gradlew build`를 실행해 확인할 것.

---

## 🔵 Phase 5 — 장기 확장
- [x] 관리자 대시보드 (전체 유저/사용량/결제 현황) — 2026-07-06 구현: `GET /api/admin/summary·users·payments` + 프론트 `/admin` 페이지. 관리자 = `ADMIN_EMAIL` 환경변수와 이메일 일치 계정(ROLE_ADMIN). **미검증**: 백엔드는 이 환경에 JDK17이 없어 컴파일 미확인 — 배포 전 `./gradlew build` 필수. Railway에 `ADMIN_EMAIL` 설정 필요
- [ ] 워터마크 자발적 달기 (크레딧 보상) — 구현 스케치는 `docs/10_수익화전략.md` 7절 (다음 우선순위 권장)
- [ ] B2B 영업 (마케팅 대행사 팀 플랜) — 착수 조건: MAU 500+ (`docs/10_수익화전략.md` 6-4절)
- [ ] 업종 확장 (헬스장, 미용실, 네일샵)
- [ ] 콘텐츠 추천 (업종/시즌별 인기 패턴)
- [ ] 팀 계정 (여러 명이 한 인스타 관리)

---

## 💰 수익화 구조 확정

### 크레딧 정책
| 항목 | 크레딧 |
|------|--------|
| 가입 시 기본 지급 | 3개 |
| 온보딩 완료 보너스 | +2개 → 총 5개 |
| 광고 시청 충전 | 1회당 1개 (하루 최대 2회) |
| 캡션 생성 차감 | -1개 |

### 플랜
| 플랜 | 가격 | 내용 |
|------|------|------|
| Free | 무료 | 크레딧 5개 + 광고 충전, 광고 있음 |
| Pro | 월 9,900원 | 무제한, 광고 없음 |

### 수익 채널
| 채널 | 시작 시점 |
|------|----------|
| Google AdSense | 배포 후 승인 시 |
| Pro 구독 (토스페이먼츠) | 배포 후 |
| B2B 대행사 팀 플랜 | 유저 확보 후 |

---

## 📋 문서 업데이트 규칙
기능 완료 시마다 업데이트:
- [ ] 00_요구사항명세서.md
- [ ] 02_기능명세서.md
- [ ] 03_화면설계서.md
- [ ] 04_DB설계서.md
- [ ] 05_API명세서.md
- [ ] 06_트러블슈팅.md
- [ ] 07_개발일지.md
- [ ] CHECKLIST.md (이 파일)

---

## 변경 이력
| 버전 | 날짜 | 내용 |
|------|------|------|
| v1.0 | 2026-07-01 | 최초 작성 |
| v1.1 | 2026-07-01 | Phase 2-2~2-5 완료 항목 체크 반영 |
| v1.2 | 2026-07-01 | Phase 2-6 에러 핸들링 완료 체크 |
| v1.3 | 2026-07-01 | 플랜 가격 확정, 크레딧 정책 확정, 관리자 이메일 알림 추가, ROADMAP.md 통합 |
| v1.4 | 2026-07-02 | 2-1 크레딧 시스템 완료, 2-3 관리자 이메일 완료, 2-4 다시 생성 버튼 완료, 2-5 약관 페이지 완료 |
| v1.5 | 2026-07-06 | Meta OAuth "Invalid Scopes" 해결, 즉시 업로드 미발행 버그 수정, 2-7 배포 항목 체크, 4-1 멀티 계정 완료 체크, 4-2/4-3 코드 작성(미검증) |
| v1.6 | 2026-07-06 | backend-node 삭제(태그 backend-node-final), Phase 5-1 관리자 대시보드 구현, docs/10_수익화전략.md 신규 |
